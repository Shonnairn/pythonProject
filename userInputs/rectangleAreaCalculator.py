# Exericse 1 Rectangle Area Calculator

print("Do you need to calculate the area for youre project? Enter the incorrect information right down below!")

length = float(input("What is the length of the stove?: "))
width = float(input("What is the width of the stove?: "))

area = length * width

# FYI to get the root number use Num lk + alt + key in 0178
print(f"The area of the stove will be: {area}cm²")
